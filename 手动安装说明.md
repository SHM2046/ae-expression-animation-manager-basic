# 手动安装 AE 动画管理器（基础版，Windows）

1. **完全退出 After Effects。**
2. 打开此路径并复制：
   `C:\Users\Administrator\AppData\Roaming\Adobe\CEP\extensions`
3. 在 `extensions` 中新建一个文件夹，名称必须是：
   `AEExpressionAnimationManagerBasic`
4. 从本插件根目录复制以下三个文件夹到刚建的文件夹内：
   - `CSXS`
   - `client`
   - `host`

完成后的目录必须是：

```text
%APPDATA%\Adobe\CEP\extensions\AEExpressionAnimationManagerBasic\CSXS\manifest.xml
%APPDATA%\Adobe\CEP\extensions\AEExpressionAnimationManagerBasic\client\index.html
%APPDATA%\Adobe\CEP\extensions\AEExpressionAnimationManagerBasic\host\animation-manager.jsx
```

5. 双击 `启用CEP调试模式.reg`，在 Windows 弹窗中选择“是”。这是未签名 CEP 开发插件被 AE 识别所必需的设置。
6. 同时把 `package.json` 与 `updater-config.json` 复制到该目录根部，以启用 GitHub 自动更新。
7. 重新打开 After Effects，在 `窗口 > Extensions > AE 动画管理器（基础版）` 中打开面板。

如果 Extensions 里仍没有显示，检查第 4 步是否多复制了一层根目录；`manifest.xml` 必须在上面列出的准确位置。
